params:
  type: object
  properties:
    FIRECRAWL_API_KEY:
      type: string
      description: Firecrawl API Key
  required:
    - FIRECRAWL_API_KEY

npx:
  command: |
    FIRECRAWL_API_KEY={FIRECRAWL_API_KEY} fastmcp run app.py 